appId: com.mawaqit.androidtv

# Tests run sequentially on the same emulator instance
# Order matters - onboarding runs first, then other tests use the set-up app
executionOrder:
  continueOnFailure: false
  flowsOrder:
    - onboarding_conditional
    - developer_mode_test
